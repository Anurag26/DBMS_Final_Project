// var express = require('express');
// var router = express.Router();
//
// var vendor_controller = require('../controllers/vendor');
//
// // a simple test url to check that all of our files are communicating correctly.
// router.get('/test', vendor_controller.test);
//
// router.post('/create', vendor_controller.vendor_create);
//
// router.get('/:id', vendor_controller.vendor_details);
//
// router.put('/:id/update', vendor_controller.vendor_update);
//
// router.delete('/:id/delete', vendor_controller.vendor_delete);
//
// module.exports = router;